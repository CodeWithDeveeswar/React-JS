// Create component using Extension: ES7+ React/Redux/React-Native snippets

// Shotcut: rafc (React Arrow Function Component)

// Shotcut: rafce (React Arrow Function Export Component)

// Shotcut: rcc (React Class Component)

import { Component } from "react";

export default class Content extends Component {
  render() {
    return <div>Content</div>;
  }
}
